export const mulShowListUser = (state, ms) => {
  state.showListUser = ms
}

export const mulShowPopupNav = (state, bool) => {
  state.showPopupNav = bool
}

export const mulCountUsers = (state, count) => {
  state.countUsers = count
}

export const mulCountUsersOnline = (state, count) => {
  state.countUsersOnline = count
}
